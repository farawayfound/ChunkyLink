# Copy this to secrets.yml, fill in real values, then encrypt:
#   ansible-vault encrypt secrets.yml
#
# Run playbook with:
#   ansible-playbook -i inventory.ini site.yml --ask-vault-pass -e @secrets.yml

sudo_password: "vpomac007"
jira_user: "replace-with-jira-username"
jira_pass: "replace-with-jira-password"
mysql_pass: "replace-with-mysql-password"

# Per-user bearer tokens — add one entry per engineer.
# token string -> display name used in access logs.
# auth_tokens:
#   "vporag-P3315113": "Jane S."
#   "vporag-P1234567": "John D."
# require_auth: true   # uncomment to block anonymous requests
