# Searches/config.ps1 — generated by Ansible. Do not edit manually.
# Re-run: ansible-playbook Ansible/Local/site.yml -e "repo_root=..."

# ══════════════════════════════════════════════════════════════════════════════
# PRIMARY SEARCH MODE
# ══════════════════════════════════════════════════════════════════════════════
$SEARCH_MODE = "{{ search_mode }}"

# ══════════════════════════════════════════════════════════════════════════════
# MCP MODE SETTINGS
# ══════════════════════════════════════════════════════════════════════════════
$MCP_SERVER_URL  = "{{ mcp_server_url }}"
$MCP_SSH_USER    = "vpomac"
$MCP_SSH_KEY     = "$HOME\.ssh\vporag_key"
$MCP_AUTH_TOKEN  = ""
$MCP_SEARCH_LEVEL = "Standard"
$MCP_MAX_RESULTS  = {{ mcp_max_results | default(5) }}
$MCP_JIRA_SOURCE  = "mysql"

# ══════════════════════════════════════════════════════════════════════════════
# LOCAL MODE SETTINGS
# ══════════════════════════════════════════════════════════════════════════════
$JSON_KB_DIR       = "{{ indexer_out_dir }}"
$JSON_SEARCH_LEVEL = "Standard"
$JSON_MAX_RESULTS  = 0

$JIRA_PRIMARY_SOURCE    = "{{ jira_primary_source }}"
$JIRA_SEARCH_ALL_SOURCES = $false

$JIRA_LOCAL_DB         = "Searches\jira_local.db"
$JIRA_LOCAL_MYSQL_HOST = "192.168.1.29"
$JIRA_LOCAL_MYSQL_PORT = 3306
$JIRA_LOCAL_MYSQL_USER = "jira_user"
$JIRA_LOCAL_MYSQL_PASS = ""
$JIRA_LOCAL_MYSQL_DB   = "jira_db"

$JIRA_DPS_CSV_DIR = "structuredData\JiraCSVexport\DPSTRIAGE"
$JIRA_RCA_CSV_DIR = "structuredData\JiraCSVexport\POSTRCA"

$JIRA_SQL_NOTE = "Connection settings in Searches/config.py — server VM0PWVPTSPL000, DB VIDPROD_MAIN"
