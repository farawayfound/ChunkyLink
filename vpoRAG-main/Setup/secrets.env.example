# MCP server credentials — copy to secrets.env and fill in values.
# secrets.env is gitignored and must never be committed.
#
# Usage: sourced automatically by Setup/Deploy-ToMCP.ps1

MCP_HOST=192.168.1.29
MCP_USER=vpomac
MCP_SSH_KEY=C:\Users\<you>\.ssh\vporag_key
MCP_SUDO_PASS=<sudo password for MCP_USER on the server>
MCP_APP_ROOT=/srv/vpo_rag
