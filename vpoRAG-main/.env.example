# Jira SQL credentials are stored in Windows Credential Manager, NOT here.
# Run once to store them securely:
#   python Searches/Connectors/jira_query.py --store-credentials
#
# This file is intentionally empty. It exists only to document the old pattern.
